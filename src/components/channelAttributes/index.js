import React, {
  Fragment,
  useMemo,
  useState,
  useEffect,
  useCallback,
  useRef,
} from "react";

const ChannelAttributes = () => {
  return <>hello attributes</>;
};

export default ChannelAttributes;
